# Copy outside Git or export these through the environment. Never put an API
# token or private key in this file.
#
# export TF_VAR_proxmox_api_token='terraform@pve!openbao=...'
# export TF_VAR_ssh_public_keys="$(jq -nc --arg key "$(cat ~/.ssh/openbao_ed25519.pub)" '[\$key]')"

start_after_create = false

