---
- name: Validate TLS and prove OpenBao is still uninitialized
  hosts: openbao_servers
  become: true
  gather_facts: false
  serial: 1
  any_errors_fatal: true

  roles:
    - role: openbao_cluster
      vars:
        openbao_cluster_validation_only: true
