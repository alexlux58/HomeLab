# Proposed repository changes — {{ expected_hostname }} ({{ ansible_host }})

* Generated: {{ ansible_date_time.iso8601 }}
* Current release: `{{ pve_upgrade_precheck_current }}`
* Target release: `{{ target_pve_release }}` (Debian {{ target_debian_suite }})
* **Nothing below has been applied.** This document exists so a human can approve it.

## Current repository state

| check | value |
|---|---|
| enterprise repo enabled | {{ pve_upgrade_precheck_repo_facts.has_enterprise }} |
| no-subscription repo enabled | {{ pve_upgrade_precheck_repo_facts.has_no_subscription }} |
| test repo enabled | {{ pve_upgrade_precheck_repo_facts.has_test }} |
| ceph repo present | {{ pve_upgrade_precheck_repo_facts.has_ceph }} |
| third-party repos | {{ pve_upgrade_precheck_repo_facts.third_party | length }} |

{% if pve_upgrade_precheck_repo_facts.third_party | length > 0 %}
### Third-party repositories found

These are not maintained by Proxmox or Debian and are the most common cause of a
failed `dist-upgrade`. Disable them for the duration of the upgrade.

{% for line in pve_upgrade_precheck_repo_facts.third_party %}
* `{{ line }}`
{% endfor %}
{% endif %}

## Current files

```text
{{ pve_upgrade_precheck_repos.stdout }}
```

## Active apt policy

Commented and disabled source entries do not appear here and are not classified
as enabled repositories.

```text
{{ pve_upgrade_precheck_repo_policy.stdout }}
```

## Proposed target state (Proxmox VE {{ target_pve_release }} / Debian {{ target_debian_suite }})

Proxmox VE 9 uses the deb822 `.sources` format. The upgrade guide's own
`sed`-based rewrite from `{{ source_debian_suite }}` to `{{ target_debian_suite }}`
is applied first, then the Proxmox repositories are converted.

`/etc/apt/sources.list.d/debian.sources`

```text
Types: deb
URIs: http://deb.debian.org/debian/
Suites: {{ target_debian_suite }} {{ target_debian_suite }}-updates
Components: main contrib non-free non-free-firmware
Signed-By: /usr/share/keyrings/debian-archive-keyring.gpg

Types: deb
URIs: http://security.debian.org/debian-security/
Suites: {{ target_debian_suite }}-security
Components: main contrib non-free non-free-firmware
Signed-By: /usr/share/keyrings/debian-archive-keyring.gpg
```

{% if pve_upgrade_precheck_repo_facts.has_enterprise %}
`/etc/apt/sources.list.d/pve-enterprise.sources` (subscription required)

```text
Types: deb
URIs: https://enterprise.proxmox.com/debian/pve
Suites: {{ target_debian_suite }}
Components: pve-enterprise
Signed-By: /usr/share/keyrings/proxmox-archive-keyring.gpg
```
{% else %}
`/etc/apt/sources.list.d/proxmox.sources` (no subscription)

```text
Types: deb
URIs: http://download.proxmox.com/debian/pve
Suites: {{ target_debian_suite }}
Components: pve-no-subscription
Signed-By: /usr/share/keyrings/proxmox-archive-keyring.gpg
```
{% endif %}

{% if pve_upgrade_precheck_repo_facts.has_enterprise and pve_upgrade_precheck_repo_facts.has_no_subscription %}
> **Conflict:** both enterprise and no-subscription repositories are enabled.
> The official documentation says not to mix them. Pick one before upgrading.
{% endif %}

## pve8to9 result

{% if pve_upgrade_precheck_current is version(pve8_minimum_before_upgrade, '<') %}
_Deferred: this host must first update from PVE {{ pve_upgrade_precheck_current }}
to at least PVE {{ pve8_minimum_before_upgrade }}. Stage 6b performs that
intermediate update, then runs and gates on `pve8to9 --full` before changing any
repository to PVE 9._
{% elif pve_upgrade_precheck_pve8to9 is defined and pve_upgrade_precheck_pve8to9 is not skipped %}
* failures: **{{ pve_upgrade_precheck_failures }}**
* warnings: **{{ pve_upgrade_precheck_warnings }}**

```text
{{ pve_upgrade_precheck_pve8to9.stdout }}
```
{% else %}
_Not run: this host is already on PVE {{ pve_upgrade_precheck_current }}._
{% endif %}

## Approval

Applying these changes requires all of:

```bash
make upgrade-146 EXTRA_JSON='{"allow_repo_change_pve146":true,"repo_change_confirmation_pve146":"{{ repo_change_confirmation_pve146_required }}","allow_upgrade_pve146":true,"upgrade_confirmation_pve146":"{{ upgrade_confirmation_pve146_required }}","console_access_confirmed_pve146":true}'
```

`console_access_confirmed_pve146` is your assertion that you have physical, IPMI
or KVM access to {{ expected_hostname }}. Do not upgrade without it: if the
network configuration does not survive the reboot, SSH will not save you.

## Rollback

There is no supported downgrade from PVE 9 to PVE 8. Rollback for this host means
**restore from the verified archives** (see docs/rollback.md), which is why stage
6b refuses to run until stage 4b has passed.
