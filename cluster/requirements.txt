# Controller-side Python tooling (macOS Ansible controller).
#   python3 -m venv .venv && . .venv/bin/activate && pip install -r requirements.txt
#
# NOTE: everything under scripts/ is standard-library only, so the migration
# tooling itself has zero runtime dependencies. The pins below are for running
# Ansible, the linters and the test-suite.
ansible-core>=2.16,<2.21
ansible-lint>=24.2.0
yamllint>=1.35.1
ruff>=0.5.0
pytest>=8.0.0
jmespath>=1.0.1
netaddr>=1.2.1
