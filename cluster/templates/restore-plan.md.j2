# Restore plan — {{ restore_plan.source_host }} → {{ restore_plan.target_node }}

* Generated: {{ ansible_date_time.iso8601 }}
* Backup generation: `{{ restore_plan.generation }}`
* Verdict: **{{ restore_plan.verdict | upper }}**
  ({{ restore_plan.counts.actionable }}/{{ restore_plan.counts.total }} entries actionable)
* Guests are restored **stopped**. Starting them is a separate approval.

## VMID mapping

| original | target | name | type | isolation | archive |
|---|---|---|---|---|---|
{% for e in restore_plan.entries %}
| {{ e.original_vmid }} | **{{ e.target_vmid }}** | {{ e.name }} | {{ e.type }} | {{ e.isolation }} | `{{ e.archive | default('-') }}` |
{% endfor %}

{% set remapped = restore_plan.entries | selectattr('target_vmid', 'ne', none) | rejectattr('original_vmid', 'equalto', 'x') | list %}
### Why anything was renumbered

VMID 9000 exists on both {{ cluster_seed_host }} and {{ restore_plan.source_host }}. In a
cluster a VMID must be unique across all nodes, so the seed keeps 9000 and the
other guest is renumbered. **Renumbering changes only the Proxmox identifier —
the guest's own IP address, hostname and configuration are untouched.**

## Network

| vmid | nic | mac | action | bridge | vlan | link down |
|---|---|---|---|---|---|---|
{% for e in restore_plan.entries %}
{% for n in e.nics %}
| {{ e.target_vmid }} | {{ n.key }} | `{{ n.mac }}` | {{ n.mac_action }} | {{ n.bridge }} | {{ n.vlan_tag | default('-') }} | {{ 'YES' if n.link_down else 'no' }} |
{% endfor %}
{% endfor %}

## Destination capacity

| storage | required | free | sufficient |
|---|---|---|---|
{% for c in restore_plan.capacity %}
| {{ c.storage }} | {{ c.required_human }} | {{ c.free_human | default('?') }} | {{ 'yes' if c.sufficient else ('unknown' if c.sufficient is none else '**NO**') }} |
{% endfor %}

## Per-guest detail

{% for e in restore_plan.entries %}
### {{ e.original_vmid }} → {{ e.target_vmid }} — {{ e.name }}

| field | value |
|---|---|
| archive | `{{ e.archive | default('-') }}` |
| sha256 | `{{ e.archive_sha256 | default('-') }}` |
| size | {{ e.archive_size_human }} |
| storage | `{{ e.restore_storage }}` |
| cpu / machine / bios | `{{ e.cpu | default('-') }}` / `{{ e.machine | default('default') }}` / `{{ e.bios | default('seabios') }}` |
| running at final backup | `{{ e.original_running_state | default('unknown') }}` |
| command | `{{ e.restore_command }}` |

{% if e.notes %}
{% for n in e.notes %}
* {{ n }}
{% endfor %}
{% endif %}
{% if e.problems %}
**Blocking problems**
{% for p in e.problems %}
* {{ p }}
{% endfor %}
{% endif %}

{% endfor %}

## Approval

```bash
make restore-180 EXTRA="-e allow_restore_pve180=true \
  -e restore_confirmation_pve180='{{ restore_confirmation_pve180_required }}'"
```

## Rollback

Nothing here deletes an archive. If a restore produces a broken guest, destroy the
restored guest only (`qm destroy <target_vmid>`), fix the plan, and restore again
from the same untouched archive.
